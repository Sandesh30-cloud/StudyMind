/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable edge runtime for faster API responses (optional)
  // experimental: { serverActions: true },
};

module.exports = nextConfig;
